---
all:
  hosts:
    k8s_master:
      ansible_host: "34.220.xx.xx" # Your STAGING AWS EC2 instance IP
      ansible_user: "ubuntu"
      ansible_ssh_private_key_file: "~/.ssh/your-aws-staging-key.pem"
  children:
    kubernetes:
      hosts:
        k8s_master: