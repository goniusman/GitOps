---
# inventory/production.yml
all:
  hosts:
    k8s_master:
      ansible_host: "54.210.xx.xx" # Put your AWS Master Node IP here
      ansible_user: "ubuntu"
      ansible_ssh_private_key_file: "~/.ssh/your-aws-key.pem"
  children:
    kubernetes:
      hosts:
        k8s_master: