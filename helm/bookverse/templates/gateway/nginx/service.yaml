apiVersion: v1
kind: Service
metadata:
  name: bookverse-nginx-gateway-service
  namespace: bookverse
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: bookverse-nginx-gateway