apiVersion: v1
kind: Service
metadata:
  name: bookverse-nginx-gateway-service
  namespace: bookverse
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30083
    protocol: TCP
    name: http
  selector:
    app: api-gateway