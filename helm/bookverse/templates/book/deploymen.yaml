apiVersion: apps/v1
kind: Deployment
metadata:
  name: book
  namespace: {{ .Values.global.namespace }}
spec:
  replicas: {{ .Values.book.replicaCount }}
  selector:
    matchLabels:
      app: book
  template:
    metadata:
      annotations:
        proxy.istio.io/config: |
          holdApplicationUntilProxyStarts: true
      labels:
        app: book
        app.kubernetes.io/name: book
        app.kubernetes.io/component: microservice
        app.kubernetes.io/part-of: bookverse
    spec:
      containers:
        - name: book
          image: "{{ .Values.book.image.repository }}:{{ .Values.book.image.tag }}"
          imagePullPolicy: Always
          ports:
            - containerPort: {{ .Values.book.service.port }}

          envFrom:
            # - configMapRef:
            #     name: book-config
            - configMapRef:
                name: app-config
            # - secretRef:
            #     name: book-secrets
            - secretRef:
                name: app-secrets

          env:
            - name: "PORT"
              value: "{{ .Values.book.service.port }}"
            - name: OTEL_SERVICE_NAME
              value: "book"
            # - name: OTEL_EXPORTER_OTLP_ENDPOINT
            #   value: "http://jaeger.bookverse.svc.cluster.local:4317"
            - name: "MONGO_URL"
              value: "mongodb://mongodb:27017/bookverse_books"

          readinessProbe:
            httpGet:
              path: /health/ready
              port: {{ .Values.book.service.port }}

          livenessProbe:
            httpGet:
              path: /health/live
              port: {{ .Values.book.service.port }}

          resources:
            requests:
              cpu: 20m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 256Mi